mypipe:
  materials:
    mygit:
      git: https://github.com/alanvg/GoCD-workshop-application.git
  stages:
    - build:
        jobs:
          build:
            tasks:
             - exec:
                 command: make
